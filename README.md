# Динамические структуры данных в языке Go
---

## Аннотация

В ходе учебной практики разработан программный модуль на языке Go, демонстрирующий применение принципов объектно-ориентированного программирования (полиморфизм, инкапсуляция, композиция), а также реализацию и использование динамических структур данных: **двусвязного списка** и **бинарного дерева поиска**. В качестве предметной области выбрана система геометрических фигур с расчётом площади и параметрическим описанием.

Проект может быть использован как учебное пособие при изучении тем «Интерфейсы в Go», «Рекурсивные структуры данных» и «Алгоритмы обхода деревьев».

---

## 3. Описание реализации

### 3.1. Подсистема геометрических фигур (пакет `figures`)

| Компонент           | Назначение                                                         |
|---------------------|--------------------------------------------------------------------|
| `Shape` (интерфейс) | Контракт с методами `Draw()`, `GetName() string`, `Area() float64` |
| `BaseShape`         | Базовая структура, инкапсулирующая поля `color` и `rotation`       |
| `Circle`            | Круг. Площадь: `π·r²`                                              |
| `Rectangle`         | Прямоугольник. Площадь: `w·h`                                      |
| `Square`            | Квадрат. Площадь: `s²`                                             |
| `Triangle`          | Треугольник. Площадь: `b·h / 2`                                    |
| `Rhombus`           | Ромб. Площадь: `d₁·d₂ / 2`                                         |

**Особенности:**

- Поворот нормализуется в диапазон `[0°; 360°)` функцией `math.Mod` с корректной обработкой отрицательных значений.
- Встраивание `BaseShape` обеспечивает наследование методов `SetColor`, `GetColor`, `SetRotation`, `GetRotation` всеми фигурами.

### 3.2. Двусвязный список (`LinkedList`)

Реализованы методы:

- `AddFirst(Shape)` - добавление в начало списка;
- `AddLast(Shape)` - добавление в конец списка;
- `Remove(*Node)` - удаление узла с корректным обновлением `head`/`tail`;
- `Print()` - вывод всех фигур;
- `PrintAreas()` - вывод только площадей.

### 3.3. Бинарное дерево поиска (`BinaryTree`)

Дерево упорядочено по возрастанию площади фигуры. Реализованы методы:

- `Insert(Shape)` - рекурсивная вставка;
- `Search(float64)` - поиск фигуры по площади с использованием эпсилон-сравнения (`|a − b| < 10⁻⁹`) для корректной работы с типом `float64`;
- `FindMin()` / `FindMax()` - поиск экстремумов;
- `Height()` - вычисление высоты дерева;
- `Size()` - подсчёт количества узлов;
- `FindInRange(min, max)` - поиск фигур в заданном диапазоне площадей;
- `ToSlice()` / `PrintInOrder()` - in-order обход.

---

## 4. Структура проекта

```
dynimic-data-structure/
├── main.go                # точка входа, демонстрация работы
├── linked_list.go         # реализация двусвязного списка
├── binary_tree.go         # реализация бинарного дерева поиска
├── go.mod                 # описание модуля Go
├── README.md              # настоящая документация
└── figures/               # доменный пакет
    ├── shape.go           # интерфейс Shape
    ├── base_shape.go      # BaseShape
    ├── circle.go
    ├── rectangle.go
    ├── square.go
    ├── triangle.go
    └── rhombus.go
```

---

## 5. Инструкция по запуску

**Требования:** Go версии 1.18 и выше.

```bash
# инициализация модуля (при отсутствии go.mod)
go mod init dynimic-data-structure

# запуск программы
go run main.go
```

---

## 6. Пример работы программы

```
Список фигур:
Triangle: base=4.00, height=2.00, color=Yellow, rotation=180.00, area=4.00
Square: side=5.00, color=Red, rotation=90.00, area=25.00
Rectangle: width=4.00, height=6.00, color=Blue, rotation=90.00, area=24.00
Circle: radius=3.00, color=Green, rotation=330.00, area=28.27
Rhombus: diagonal1=6.00, diagonal2=4.00, color=Orange, rotation=0.00, area=12.00

Площади фигур:
Triangle area = 4.00
Square area = 25.00
Rectangle area = 24.00
Circle area = 28.27
Rhombus area = 12.00

Двоичное дерево фигур по площади:
Triangle area = 4.00
Rhombus area = 12.00
Rectangle area = 24.00
Square area = 25.00
Circle area = 28.27
```
